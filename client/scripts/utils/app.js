// точка входа в прило.
// импорты, конфиги, загрузка приложения.
// Логики не должно быть. Вызов, старт

// выделить главную страницу

// пути в логине не настроены

import {Router} from "./router.js";

class App {
    constructor() {
        new Router();
    }
}

(new App());

